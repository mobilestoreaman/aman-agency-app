[build]
builder = "nixpacks"
buildCommand = "go build -o backend-server ./cmd/server"

[deploy]
startCommand = "./backend-server"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
