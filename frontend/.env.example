# ── Frontend Environment Variables ───────────────────────────────────────────
# Copy to .env.local for local development

# API base URL — defaults to /api/v1 via Vite proxy when empty
VITE_API_BASE_URL=

# App display name (used in page titles)
VITE_APP_NAME=Aman Agency
